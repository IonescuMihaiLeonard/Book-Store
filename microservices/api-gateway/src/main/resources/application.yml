server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: api-gateway

services:
  auth-url: ${AUTH_SERVICE_URL:http://localhost:8081}
  catalog-url: ${CATALOG_SERVICE_URL:http://localhost:8082}
  order-url: ${ORDER_SERVICE_URL:http://localhost:8083}
